export namespace Client {
  export type Params = {
    name: string
    email: string
    password: string
  }
  export type Model = {
    id: string
    name: string
    email: string
    password: string
    amount: number
  }
}
